To create environment

./setup.sh

To install all the packages, in active env, mentioned in the requirements.txt
pip install -r requirements.txt

Open new terminal. To run mcp server
python3 fastmcp_server.py

Open new terminal. To run mcp client
python3 fastmcp_client.py
